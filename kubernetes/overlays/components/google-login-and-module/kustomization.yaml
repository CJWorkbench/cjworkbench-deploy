apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

patches:
- path: google-config.yaml
  target:
    name: frontend-deployment  # for login
- path: google-config.yaml
  target:
    name: fetcher-deployment  # for module-fetch
